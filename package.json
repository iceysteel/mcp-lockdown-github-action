{
  "name": "mcp-security-action",
  "version": "1.0.0",
  "description": "GitHub Action to scan MCP servers for security vulnerabilities using the SAFE-MCP framework",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "lint": "tsc --noEmit"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mcplockdown/mcp-security-action.git"
  },
  "keywords": [
    "github-action",
    "mcp",
    "security",
    "vulnerability-scanner",
    "safe-mcp"
  ],
  "author": "MCP Lockdown",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.0",
    "@vercel/ncc": "^0.38.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.4",
    "typescript": "^5.4.5"
  }
}
